/**
 *  1. Create a promise object using Promise() constructor.
 *  2. Promise constructor take a function as an argument.
 *  3. The function also accepts two functions : resolve & reject.
 *  4. If the promise returns successfully, the resolve function is called.
 *  5. If the promise have an error occurs, the reject function is called.
 *
 * **/


let promise = new Promise(function(resolve, reject){

});

